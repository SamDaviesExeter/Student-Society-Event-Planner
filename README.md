# Student-Society-Event-Planner
Student Society Event Planner Coursework

Requires Python 3.x with no additional external libraries

To run the program:
  Ensure that the plain text files are inside the folder named assets in the same directory as the script
  Then execute the main script
